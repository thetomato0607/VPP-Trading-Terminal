[pytest]
# Make both the repo root (for `modules`, `app`, etc.) and backend/ (for
# `main`, `models`, `routes`, etc.) importable regardless of which directory
# pytest is invoked from. Requires pytest>=7.0 (pinned to 7.4.0 in
# requirements.txt).
pythonpath = . backend
